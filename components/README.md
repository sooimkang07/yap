# Components

Standalone UI modules that are more self-contained than the main `js/` app code.

## Current Component

- `voice-visualizer/`: recording visualizer with runtime files used by the web app and source files kept for future edits
