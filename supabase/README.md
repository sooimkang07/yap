# Supabase

Database source of truth for the app.

## Files

- `schema.sql`: full schema for a fresh project
- `migrate.sql`: additive migration for an existing project
- `seed.sql`: starter records for local/demo setup
